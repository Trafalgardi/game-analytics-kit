---
name: Bug in the engine or the installer
about: A command fails, a number is computed wrong, install/update breaks something
labels: bug
---

**What happened**
<!-- The command and its output (one `error: ...` line is enough). Remove tokens, app ids, SDK keys and device ids. -->

**What you expected**

**Environment**
- Kit version (`py analytics/ga.py status`, first line):
- OS and Python version:
- Agent (Claude Code / Codex) and its version, if an agent ran the command:

**Data**
<!-- Size of the database, sample rate, the tables involved. No raw rows from a real game. -->
